import { TextHideDirective } from './text-hide.directive';

describe('TextHideDirective', () => {
  it('should create an instance', () => {
    const directive = new TextHideDirective();
    expect(directive).toBeTruthy();
  });
});
